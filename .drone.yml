---
kind: pipeline
type: docker
name: default

steps:
  - name: testing
    image: golang:1.17
    commands:
      - make deps
      - make test
    when:
      branch:
          include:
            - tests
